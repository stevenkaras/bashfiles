#!/bin/usr/env bash

function notify() {
    osascript -e "display notification \"$*\" with title \"Terminal\""
}

alias lstat='stat -L'
alias trim_trailing_spaces='sed -i "" -E -e '"'"'s/[[:space:]]*$//'"'"''
